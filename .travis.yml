language: node_js
node_js: '8'

cache:
  directories:
    - "node_modules"
env:
  matrix:
  - PROVIDER=amazon
before_install:
- npm install -g serverless
script:
- cp -R functions $PROVIDER/
- cd $PROVIDER
- sls deploy
